<h1> Articles </h1>

<% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p> <%= link_to "Delete article", article_path(article) %></p>
  <p><%= article.content %></p>
<% end %>

